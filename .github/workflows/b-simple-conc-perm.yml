name: WFVAR-B

on:
  push:
    branches: [fix/ci-startup-failure]

concurrency:
  group: wfvarb-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - run: echo ok
