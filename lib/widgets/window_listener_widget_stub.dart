// Web stub for WindowListenerWidget - not available on web
import 'package:flutter/material.dart';

/// Stub widget that does nothing on web platform
class WindowListenerWidget extends StatelessWidget {
  final Widget child;

  const WindowListenerWidget({super.key, required this.child});

  @override
  Widget build(BuildContext context) => child;
}
