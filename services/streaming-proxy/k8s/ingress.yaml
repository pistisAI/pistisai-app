apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: streaming-proxy-ingress
  namespace: CloudToLocalLLM
  annotations:
    kubernetes.io/ingress.class: "gce"
    kubernetes.io/ingress.global-static-ip-name: "CloudToLocalLLM-static-ip"
    networking.gke.io/managed-certificates: "CloudToLocalLLM-certificate"
spec:
  rules:
  - host: streaming.pistisai.app
    http:
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: streaming-proxy
            port:
              number: 8080
